[project]
name = "adescent"
version = "0.1.0"
description = "Reproducible simulations for activity-space kernel descent"
requires-python = ">=3.14"
dependencies = [
    "matplotlib>=3.10.9",
    "numpy>=2.4.6",
    "snakemake>=9.0",
]
